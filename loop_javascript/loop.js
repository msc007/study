/* basic for loop:
 */
for(let i = 0; i < length; i++){
}

/* basic while loop:
 */
while(true){
}

/* for...of
 * iterate iterable (array, string)
 */
for(let char of str){
}

/* for...in
 * iterate over the properties of object (key, index)
 */
for (let index in str) {
    console.log(`Index of ${str[index]}: ${index}`);
}